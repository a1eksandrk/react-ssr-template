import React from "react"

import Counter from "./Counter"

export default App = () => (
    <main>
        <section>
            <h1>React SSR Template</h1>
            <Counter />
        </section>
    </main>
)
